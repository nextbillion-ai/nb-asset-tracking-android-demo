<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NbAssetTrackingDemo"
        tools:targetApi="31">
        <activity
            android:name=".CodeExampleList"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.NbAssetTrackingDemo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AssetProfileOperations"
            android:exported="true"/>

        <activity
            android:name=".SimpleTrackingExample"
            android:exported="true" />

        <activity
            android:name=".UpdateConfiguration"
            android:exported="true" />

        <activity
            android:name=".GetAssetCallback"
            android:exported="true" />

        <activity
            android:name=".SetProfileActivity"
            android:exported="true" />

        <activity
            android:name=".ExtendedTrackingActivity"
            android:exported="true" />


    </application>

</manifest>